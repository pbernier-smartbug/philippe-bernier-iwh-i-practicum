doctype html
html 
    head 
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body    
        h1 Custom Object Table
        a(href="/update-cobj")
                | Add to this table
        table.table
            tr
              th Name
              th Specie
              th Race
            each pets in data
              tr
                td #{pets.properties.name}
                td #{pets.properties.specie}
                td #{pets.properties.race}